// Utilize o for/of para imprimir os elementos da lista names com o console.log()

let names = ['João', 'Maria', 'Antônio', 'Margarida'];

for (let list of names) { //python: for list in names:
    console.log(list);    //           print(list)
    
}
